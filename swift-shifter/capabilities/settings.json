{
  "$schema": "../node_modules/@tauri-apps/cli/schema/capability.json",
  "identifier": "settings",
  "description": "Permissions for the Settings / Preferences window",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:window:allow-close"
  ]
}
